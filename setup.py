"""Setup file for the sqrl package."""
import setuptools

setuptools.setup(
    name='sqrl',
    version='1.0.0',
    description=' A schedule builder for the University of Toronto.',
    packages=setuptools.find_packages(),
    python_requires='>=3.9',
)
